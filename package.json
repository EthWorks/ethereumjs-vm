{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "test:prepare": "./scripts/test-prepare.sh && lerna bootstrap --npm-client=yarn",
    "ci:prepare": "./scripts/test-prepare.sh && lerna bootstrap --ignore-scripts --no-ci"
  }
}
